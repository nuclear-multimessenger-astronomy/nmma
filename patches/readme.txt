patches are installed best on the following branches of bilby and parallel-bilbybefore running "git apply *.patch" in respective repo folders 

for bilby, use:
git checkout -b branch_1.1.4 1.1.4

for parallel-bilby, use:
git checkout -b branch_1.0.1 1.0.1


