config:
  withTime:
    value: false
    filters:
      - null # you can keep it or remove it, it will still be parsed as None (filter independent)
    time_nodes: 4
    type: "Uniform"
    min: 0
    max: 2
  withoutTime:
    value: true
    type: "Uniform"
    min: 0
    max: 2
